import {Component} from 'react'
import {connect} from 'react-redux'

export default class CropsContainer extends Component {
    render (){
        return(
            <div>Crops Container</div>
        )
    }
}